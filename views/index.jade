extends layout
block content
    include header
    h1= title
    if(title!="Not loged in")
        p Welcome, #{title}
    button#login.change_userSettings(onclick="window.location ='/login'") Login
    if(title!="Not logged in")
        button#remove_user.change_userSettings(onclick="window.location ='/logout'") Log out
        button#change_userSettings.change_userSettings(onclick='window.location="/user/#{login}"') My Profile


    //img(src="images/feature-1.png")
    include footer
