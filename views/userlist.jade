extends layout


block content
    h3.
        Users
    ul
        each user, i in userlist
            li#user_list_item
                a(href='/user/#{user.login}') #{user.user} with login #{user.login} and password #{user.password}