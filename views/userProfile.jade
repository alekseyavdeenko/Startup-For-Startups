
extends layout

block content
    include header
    h1=title


    if(logedUser!=null)
        button#change_userSettings(onclick='window.location="/user/#{logedUser.login}/profileSettings"') Profile Settings